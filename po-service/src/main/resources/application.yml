server:
  port: 8084

spring:
  application:
    name: po-service
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27020/po_db?w=1}

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka/}
