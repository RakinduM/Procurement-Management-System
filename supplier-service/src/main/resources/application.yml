server:
  port: 8082

spring:
  application:
    name: supplier-service
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27018/supplier_db?w=1}

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka/}
